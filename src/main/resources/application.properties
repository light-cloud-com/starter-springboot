spring.application.name=starter-springboot

# Cloud Run provides the port to listen on in $PORT. Spring Boot reads
# server.port, so without this the app listens on 8080 regardless of what the
# platform asked for.
server.port=${PORT:8080}
